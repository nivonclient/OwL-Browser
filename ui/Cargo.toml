[package]
name = "owl-ui"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

description = "IPC bridge for the HTML/CSS browser UI."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
